services:
  - type: web
    name: aeris-ground-control
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-dashboard.txt
    startCommand: streamlit run aeris/dashboard/app.py --server.address=0.0.0.0 --server.port=$PORT
    healthCheckPath: /_stcore/health